schema_version: 0
artifact_type: dmeta_widget_template_package
summary: Street Deli local widget-template package index.
long_summary: Street Deli keeps local templates near the example so the concrete instantiation can evolve independently of
  the global DMETA template catalog.
status: draft
files:
  index: ./widget-templates/00-index.yaml
  ordering_flow: ./widget-templates/ordering-flow.yaml
validation:
  require_unique_template_ids: true
