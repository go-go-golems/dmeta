// Code generated by dmeta scaffold-instance from a DMETA instance manifest. DO NOT EDIT until promoted.

// This file is an implementation scaffold. Promote and edit intentionally.
// It is not exported from the component barrel to avoid accidental runtime imports.

import type { StreetDeliSubstitutionChipProps } from "./StreetDeliSubstitutionChip.types";

/**
 * TODO adapter for `deli.substitution_chip`.
 *
 * Semantic context:
 * Archetypes:
 * - Substitution
 * Capabilities:
 * - role_preserving_substitutable
 * - dietary_substitutable
 * - price_aware_substitutable
 * Presentations:
 * - substitution_badge
 * - substitution_pair
 *
 * Resolved inherited context:
 * - Archetype Substitution (ancestors: Archetype -> Entity -> Relation)
 *   effective capabilities: identifiable, labelable, inspectable, relatable, substitutable
 * - Capability role_preserving_substitutable (ancestors: Capability -> substitutable)
 *   effective projections: allergen_flags, auto_suggest, dietary_compatibility, flavor_fit, price_delta_cents, replacement_candidates, replaces, role_overlap_score, role_preservation
 * - Capability dietary_substitutable (ancestors: Capability -> substitutable -> role_preserving_substitutable -> dietary)
 *   effective projections: allergen_contains, allergen_flags, allergen_may_contain, auto_suggest, dietary_compatibility, dietary_explanation, dietary_tags, flavor_fit, price_delta_cents, replacement_candidates, replaces, role_overlap_score, role_preservation
 * - Capability price_aware_substitutable (ancestors: Capability -> substitutable -> role_preserving_substitutable -> measurable)
 *   effective projections: allergen_flags, auto_suggest, dietary_compatibility, flavor_fit, price_delta_cents, price_delta_label, replacement_candidates, replaces, role_overlap_score, role_preservation, unit, value
 * - Presentation substitution_badge (role: badge)
 * - Presentation substitution_pair (role: substitution_pair)
 *
 * Projection hints:
 * Recommended:
 * - substitutable.replaces
 * - substitutable.replacement_candidates
 * - role_preserving_substitutable.role_overlap_score
 * Optional:
 * - dietary_substitutable.dietary_explanation
 * - price_aware_substitutable.price_delta_label
 * - dietary.dietary_tags
 */
export function mapDomainToStreetDeliSubstitutionChipProps(input: unknown): StreetDeliSubstitutionChipProps {
  return {
    // TODO: consider mapping recommended projection hint substitutable.replaces
    // TODO: consider mapping recommended projection hint substitutable.replacement_candidates
    // TODO: consider mapping recommended projection hint role_preserving_substitutable.role_overlap_score
    // TODO: optionally map projection hint dietary_substitutable.dietary_explanation when this variant needs it
    // TODO: optionally map projection hint price_aware_substitutable.price_delta_label when this variant needs it
    // TODO: optionally map projection hint dietary.dietary_tags when this variant needs it
    // TODO: Choose whether the chip shows only top replacement or opens alternatives.
    // TODO: Do not hide allergen warnings when the selected candidate introduces risk.
    subject: input,
  } as unknown as StreetDeliSubstitutionChipProps;
}
