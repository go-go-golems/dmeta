// Code generated by dmeta scaffold-instance from a DMETA instance manifest. DO NOT EDIT until promoted.

// This file is an implementation scaffold. Promote and edit intentionally.
// It is not exported from the component barrel to avoid accidental runtime imports.

import type { StreetDeliOrderCartProps } from "./StreetDeliOrderCart.types";

/**
 * TODO adapter for `deli.order_cart`.
 *
 * Semantic context:
 * Archetypes:
 * - OrderItem
 * - WorkItem
 * - ProductComposition
 * Capabilities:
 * - ingredient_composable
 * - configurable
 * - measurable
 * - stateful
 * Presentations:
 * - order_item_row
 * - compact_ref
 *
 * Projection hints:
 * Recommended:
 * - labelable.label
 * - measurable.value
 * - ingredient_composable.parts
 * - configurable.current_config
 * Optional:
 * - stateful.state
 * - relatable.work_item_ref
 */
export function mapDomainToStreetDeliOrderCartProps(input: unknown): StreetDeliOrderCartProps {
  return {
    // TODO: consider mapping recommended projection hint labelable.label
    // TODO: consider mapping recommended projection hint measurable.value
    // TODO: consider mapping recommended projection hint ingredient_composable.parts
    // TODO: consider mapping recommended projection hint configurable.current_config
    // TODO: optionally map projection hint stateful.state when this variant needs it
    // TODO: optionally map projection hint relatable.work_item_ref when this variant needs it
    // TODO: Map applied substitutions as cart-line annotations, not as raw ingredient rows only.
    // TODO: Decide whether stateful prep status appears in cart or only in tracker.
    subject: input,
  } as unknown as StreetDeliOrderCartProps;
}
