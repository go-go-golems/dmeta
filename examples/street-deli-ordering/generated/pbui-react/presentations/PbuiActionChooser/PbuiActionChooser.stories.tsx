// Generated by DMETA PBUI React target. Do not edit by hand.

import type { Meta, StoryObj } from '@storybook/react';
import { PbuiActionChooser } from './PbuiActionChooser';

const meta = {
  title: 'PBUI/PbuiActionChooser',
  component: PbuiActionChooser,
  parameters: {
    docs: {
      description: {
        component: "CLIM-style systems make available commands visible and selectable. When an object has a compact reference plus inspection/copy affordances, PBUI should be able to present compatible actions as first-class objects without waiting for a specific target menu or toolbar design. The v1 rule intentionally keys off the currently emitted `copy_reference` and `inspect_subject` actions instead of requiring a separate `select_subject` obligation, because Street Deli domain-specific selection currently appears as actions such as `select_menu_item` rather than a universal selection obligation.",
      },
    },
  },
} satisfies Meta<typeof PbuiActionChooser>;

export default meta;
type Story = StoryObj<typeof meta>;

export const Default: Story = {
  args: {
    label: "pbui.action_chooser",
  },
};
