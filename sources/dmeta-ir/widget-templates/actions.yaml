schema_version: 0
artifact_type: dmeta_widget_templates
category: actions
summary: DMETA actions widget templates.
long_summary: Templates in this file are selectable/adaptable actions starting points for concrete DMETA instances.
templates:
- id: dmeta.action_palette
  name: ActionPalette
  status: template
  classification:
    level: organism
    role: typed_action_discovery
  intent:
    purpose: Discover actions globally or for a selected PresentationRef and collect typed arguments.
    adapter_boundary: Receives generated action metadata, current selection, and candidate presentations.
  consumes:
    presentations:
    - compact_ref
    - inline_token
    - status_badge
    - metric_cell
  contract:
    props:
      ActionPaletteProps:
        fields:
          open:
            type: boolean
            required: true
          source:
            type: PresentationRef
            required: false
          actions:
            type: ActionDefinition[]
            required: true
          candidates:
            type: PresentationRef[]
            required: false
    action_slots:
      onChooseAction:
        accepts: ActionDefinition
      onChooseArgument:
        accepts: PresentationRef
      onExecute:
        accepts: PresentationActionRequest
  stories:
  - global
  - source_context
  - awaiting_argument
  - candidates
  - empty
  outputs:
    component: src/dmeta/widgets/ActionPalette/ActionPalette.tsx
    types: src/dmeta/widgets/ActionPalette/ActionPalette.types.ts
    metadata: src/dmeta/widgets/ActionPalette/ActionPalette.metadata.ts
    stories: src/dmeta/widgets/ActionPalette/ActionPalette.stories.tsx
    barrel: src/dmeta/widgets/ActionPalette/index.ts
  template:
    category: actions
    selection: optional
    maturity: draft
    default_importance: common
    selection_questions:
    - Does this instance need ActionPalette as a concrete component or can another selected template cover the same role?
    adaptation_points:
    - component_name
    - props
    - stories
    - presentation_mapping
    - action_slots
    common_variants:
    - default
    avoid_when:
    - The concrete instance has no workflow matching this template role.
- id: dmeta.action_button
  name: ActionButton
  status: template
  classification:
    level: atom
    role: action_button
  intent:
    purpose: Render one action with semantic tone, loading/disabled state, and adapter-boundary callback.
    adapter_boundary: Receives normalized view models and typed semantic refs; emits typed callbacks; does not parse raw backend
      JSON or own trusted side effects.
  template:
    category: actions
    selection: common
    maturity: draft
    default_importance: common
    selection_questions:
    - Does this concrete instance need this widget behavior, or is it better represented by a domain-specific local template?
    - Which selected presentations/actions should this template expose?
    adaptation_points:
    - component_name
    - props
    - stories
    - presentation_mapping
    - action_slots
    - density
    - empty_state
    common_variants:
    - default
    avoid_when:
    - The instance has no workflow matching this template role.
    - A more specific local template better expresses the product interaction.
  consumes:
    capabilities:
    - actionable
  contract:
    props:
      ActionButtonProps:
        fields:
          action:
            type: ActionDefinition
            required: true
          tone:
            type: SemanticTone
            required: false
    action_slots:
      onExecute:
        accepts: PresentationActionRequest
  stories:
  - primary
  - secondary
  - danger
  - loading
  outputs:
    component: src/dmeta/widgets/ActionButton/ActionButton.tsx
    types: src/dmeta/widgets/ActionButton/ActionButton.types.ts
    metadata: src/dmeta/widgets/ActionButton/ActionButton.metadata.ts
    stories: src/dmeta/widgets/ActionButton/ActionButton.stories.tsx
    barrel: src/dmeta/widgets/ActionButton/index.ts
- id: dmeta.action_group
  name: ActionGroup
  status: template
  classification:
    level: molecule
    role: action_group
  intent:
    purpose: Render a group of primary/secondary actions for a selected subject, panel, page, or bulk selection.
    adapter_boundary: Receives normalized view models and typed semantic refs; emits typed callbacks; does not parse raw backend
      JSON or own trusted side effects.
  template:
    category: actions
    selection: common
    maturity: draft
    default_importance: common
    selection_questions:
    - Does this concrete instance need this widget behavior, or is it better represented by a domain-specific local template?
    - Which selected presentations/actions should this template expose?
    adaptation_points:
    - component_name
    - props
    - stories
    - presentation_mapping
    - action_slots
    - density
    - empty_state
    common_variants:
    - default
    avoid_when:
    - The instance has no workflow matching this template role.
    - A more specific local template better expresses the product interaction.
  consumes:
    capabilities:
    - actionable
  contract:
    props:
      ActionGroupProps:
        fields:
          actions:
            type: ActionDefinition[]
            required: true
          subject:
            type: PresentationRef
            required: false
    action_slots:
      onExecute:
        accepts: PresentationActionRequest
  stories:
  - row_actions
  - panel_actions
  - bulk_actions
  outputs:
    component: src/dmeta/widgets/ActionGroup/ActionGroup.tsx
    types: src/dmeta/widgets/ActionGroup/ActionGroup.types.ts
    metadata: src/dmeta/widgets/ActionGroup/ActionGroup.metadata.ts
    stories: src/dmeta/widgets/ActionGroup/ActionGroup.stories.tsx
    barrel: src/dmeta/widgets/ActionGroup/index.ts
- id: dmeta.context_menu_trigger
  name: ContextMenuTrigger
  status: template
  classification:
    level: atom
    role: context_menu_trigger
  intent:
    purpose: Expose secondary presentation actions without adding dense visual noise.
    adapter_boundary: Receives normalized view models and typed semantic refs; emits typed callbacks; does not parse raw backend
      JSON or own trusted side effects.
  template:
    category: actions
    selection: common
    maturity: draft
    default_importance: common
    selection_questions:
    - Does this concrete instance need this widget behavior, or is it better represented by a domain-specific local template?
    - Which selected presentations/actions should this template expose?
    adaptation_points:
    - component_name
    - props
    - stories
    - presentation_mapping
    - action_slots
    - density
    - empty_state
    common_variants:
    - default
    avoid_when:
    - The instance has no workflow matching this template role.
    - A more specific local template better expresses the product interaction.
  consumes:
    presentations:
    - compact_ref
    - inline_token
    - status_badge
    - metric_cell
    capabilities:
    - actionable
  contract:
    props:
      ContextMenuTriggerProps:
        fields:
          subject:
            type: PresentationRef
            required: true
          actions:
            type: ActionDefinition[]
            required: true
    action_slots:
      onOpenMenu:
        accepts: PresentationRef
      onExecute:
        accepts: PresentationActionRequest
  stories:
  - token_menu
  - row_menu
  - disabled
  outputs:
    component: src/dmeta/widgets/ContextMenuTrigger/ContextMenuTrigger.tsx
    types: src/dmeta/widgets/ContextMenuTrigger/ContextMenuTrigger.types.ts
    metadata: src/dmeta/widgets/ContextMenuTrigger/ContextMenuTrigger.metadata.ts
    stories: src/dmeta/widgets/ContextMenuTrigger/ContextMenuTrigger.stories.tsx
    barrel: src/dmeta/widgets/ContextMenuTrigger/index.ts
